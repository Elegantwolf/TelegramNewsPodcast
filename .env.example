# Copy this file to .env or export these values directly in your shell.
# This example intentionally contains placeholders only.

TELEGRAM_API_ID=your_telegram_api_id
TELEGRAM_API_HASH=your_telegram_api_hash

# Optional. Keep the session on the local client, outside any NAS archive.
TELEGRAM_SESSION_PATH=~/.config/telegram-news-podcast/telegram.session
